<h1><%= question_header(@question) %></h1>

<%= render 'form' %>
<%= link_to 'Back', test_path(@question) %>
<%= link_to 'Delete', question_path(@question),
            method: :delete,
            data: { confirm: 'Are you sure?' } %>