<%= render 'form' %>
<%= link_to t('helpers.back'), admin_test_path(@question) %>
<%= link_to t('helpers.delete'), question_path(@question),
            method: :delete,
            data: { confirm: t('helpers.delete_confirmation') } %>