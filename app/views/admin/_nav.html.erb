<div class="nav nav-admin">
  <span class="nav-admin title"><%= t('.header') %></span>

  <div class="nav-admin user">
    <%= t('helpers.welcome') %> <b class="nav-admin user-email"><%= current_user.email %></b> |
    <%= link_to t('helpers.logout'), destroy_user_session_path, method: :delete %>
  </div>
</div>