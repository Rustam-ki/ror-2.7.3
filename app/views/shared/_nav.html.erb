<nav class="navbar navbar-light bg-light">
  <%= link_to 'TestGuru', root_path, class: 'navbar-brand' %>

  <span class="navbar-text">
    <% if user_signed_in? %>
      <%= link_to 'My badges', badges_path, class: 'nav-link active' %>
      <%= t('helpers.welcome') %> <b><%= current_user.email %></b> Guru |
      <%= link_to t('helpers.logout'), destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to t('helpers.login'), new_session_path(resource_name) %> |
      <%= link_to t('helpers.signup'), new_registration_path(resource_name) %>
    <% end %>
  </span>
</nav>
