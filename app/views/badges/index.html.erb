<h1>Badges</h1>

<table class="table table-hover">
  <tr class="table-primary">
    <th class="align-middle">Title</th>
    <th class="align-middle">Image</th>
    <th class="align-middle">Date</th>
  </tr>
  <%= render @badges  %>
</table>

<p>
  <%= link_to 'Back', root_path %>
</p>

<%= render 'badges/all_badges'%>